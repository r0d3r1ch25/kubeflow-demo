apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-envoy
  namespace: kubeflow
spec:
  selector:
    matchLabels:
      app: metadata-envoy
  template:
    metadata:
      labels:
        app: metadata-envoy
    spec:
      containers:
      - name: container
        image: gcr.io/ml-pipeline/metadata-envoy:2.0.0
        ports:
        - containerPort: 9090